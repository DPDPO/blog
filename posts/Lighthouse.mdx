---
title: Lighthouse 분석 결과 항목
date: 2023-05-31
description: Optimization
---

# **Lighthouse 분석 결과 항목**

## 1. Performance

Performance 항목에서는 웹 성능을 측정합니다. 화면에 콘텐츠가 표시되는데 시간이 얼마나 걸리는지, 표시된 후 사용자와 상호작용하기 까진 얼마나 걸리는지, 화면에 불안정한 요소는 없는지 등을 확인합니다.

<br />
### -Opportunities <br />
Serve images in next-gen formats WebP 또는 AVIF 이미지 포맷 사용하기

<br />
<br />
```tsx
ex)
<picture>
  <source srcset="logo.webp" type="image/webp">
  <img src="logo.png" alt="logo">
</picture>
```

<br />
<br />
#### Reduce unused JavaScript 안쓰는 자바스크립트 코드 줄이기

<br />
<br />
#### Reduce unused CSS 안쓰는 css 코드 줄이기

<br />
<br />
#### Eliminate render-blocking resources 렌더링 차단 리소스 제거

<br />

> 렌더링 차단 리소스는 scripts 그리고 stylesheets
> 중요한 코드를 식별됐으면 해당 스크립트 태그에서 async 또는 defer 속성을 추가하면 된다. 보통 async 속성은 다른 파일보다 순서가 중요할때 사용된다

<br /> 스타일시트는 스크립트 태그와 동일하게 비동기로 로드하면 된다. HTML 페이지
head 태그에 FCP에만 필요한 스타일만 로드하고 그 외에는 preload 속성으로 비동기로
처리하게 만들면 된다.

<br />
<br />
  
```tsx
ex)
<script async src="high-priority.js"></script>
<script defer src="general.js"></script>
  
<link rel="preload" href="styles.css" as="style">
```

<br />
<br />

#### Preconnect to required origins

사전연결

<br />
```tsx
ex) 
<link rel="preconnect" href="https://example.com">
```

<br />
<br />
## 2. Accessibility Accessibility 항목에서는 웹 페이지가 웹 접근성을 잘 갖추고
있는지 확인합니다. 대체 텍스트를 잘 작성했는지, 배경색과 콘텐츠 색상의 대비가
충분한지, 적절한 WAI-ARIA 속성을 사용했는지 등을 확인합니다.

<br />
<br />
## 3. Best Practices Best Practices 항목에서는 웹 페이지가 웹 표준 모범 사례를
잘 따르고 있는지 확인합니다. HTTPS 프로토콜을 사용하는지, 사용자가 확인할 확률은
높지 않지만 콘솔 창에 오류가 표시 되지는 않는지 등을 확인합니다.

<br />
<br />
## 4. SEO SEO 항목에서는 웹 페이지가 검색 엔진 최적화가 잘 되어있는지
확인합니다. 애플리케이션의 robots.txt가 유효한지, meta 요소는 잘 작성되어
있는지, 텍스트 크기가 읽기에 무리가 없는지 등을 확인합니다.

<br />
<br />
## 5. PWA (Progressive Web App) PWA 항목에서는 해당 웹 사이트가 모바일
애플리케이션으로서도 잘 작동하는지 확인합니다. 앱 아이콘을 제공하는지, 스플래시
화면이 있는지, 화면 크기에 맞게 콘텐츠를 적절하게 배치했는지 등을 점수가 아닌
체크리스트로 확인합니다.
