import Container from "@/components/container";
import React from "react";

const Blog = () => {
  return (
    <Container>
      <span className={`font-bold`}>BLOG</span>
    </Container>
  );
};

export default Blog;
